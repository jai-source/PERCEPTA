import { useState } from 'react';
import DashboardPanel from '../components/DashboardPanel';
const steps = ['Neutral head position', 'Eyebrow raise', 'Blink and wink', 'Hand gesture stability', 'Microphone check'];
export default function Calibration() { const [step, setStep] = useState(0); return <div className="page"><div className="page-intro"><div><p className="eyebrow">PERSONALIZATION / GUIDED FLOW</p><h2>Calibration lab</h2></div><span className="connection-pill connected">STEP {step + 1} / {steps.length}</span></div><DashboardPanel title={steps[step]} eyebrow="ACTIVE PROCEDURE" className="calibration-panel"><div className="calibration-visual"><div className="calibration-ring"><span>{step + 1}</span></div><p>{step === 0 ? 'Look straight at the camera and settle into a neutral position.' : 'Follow the prompt and hold the signal until the temporal filter confirms it.'}</p></div><div className="stepper">{steps.map((label, index) => <button className={index === step ? 'current' : index < step ? 'complete' : ''} key={label} onClick={() => setStep(index)}><span>{index < step ? '✓' : index + 1}</span>{label}</button>)}</div><button className="btn btn-primary" onClick={() => setStep((step + 1) % steps.length)}>{step === steps.length - 1 ? 'Restart calibration' : 'Mark detected'}</button></DashboardPanel></div>; }
